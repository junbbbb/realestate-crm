/** Default page size for property listings */
export const DEFAULT_PAGE_SIZE = 50;

/** Maximum number of properties to compare side-by-side */
export const MAX_COMPARE_COUNT = 5;

/** Debounce delay for search input (ms) */
export const SEARCH_DEBOUNCE_MS = 300;

/** Fallback userId for PIN-authenticated (single-user) sessions */
export const PIN_USER_ID = "pin-user";

/** Max crawl logs to keep in settings */
export const MAX_CRAWL_LOGS = 100;

/** Position increment for kanban card ordering */
export const KANBAN_POSITION_GAP = 1024;
